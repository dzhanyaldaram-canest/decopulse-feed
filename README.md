# decopulse-feed

Published item feed for the DecoPulse iOS app. `items.json` is generated
weekly by the app repo's scraper workflow, which live-validates every image
and vendor link before publishing. `report.json` describes the last run.

Served via GitHub Pages: https://dzhanyaldaram-canest.github.io/decopulse-feed/items.json
